# Artifact manifest

This handoff includes all meaningful text/source artifacts produced during the investigation that were present in the working directory on 2026-08-06.

Included in the complete archive:

- all research notes through v5;
- the corrected current-status note;
- the hostile LongCat audit and counterexamples;
- all available C++ source scanners and analyzers;
- all nonempty raw run outputs;
- reproducibility notes;
- historical checksum manifests.

Intentionally excluded:

- compiled native binaries, because they are platform-specific and reproducible from source;
- temporary checkpoint files `tmp_8500000.txt` and `tmp_9000000.txt`;
- zero-byte incomplete outputs `primorial_runnerup_scan_m8_5e6.txt` and `primorial_runnerup_scan_m1e7.txt`.

The complete snapshot is stored at `artifacts/suns-conjecture-research-2026-08-06.zip` with a SHA-256 sidecar.
